/* NICE SPACESHIP — Theme Lite (matches app dark theme, no toggle needed) */
const ThemeLite = (() => {
  function init() {
    // Community site always uses the app's default dark theme.
    // No light/dark toggle — consistent with the app experience.
  }
  function toggle() {}
  function set() {}
  return { init, set, toggle };
})();
